jdk:
  - openjdk11

install:
  - echo "Running a custom install command"
  - ./gradlew clean -Pgroup=com.github.kotlin.graphics assemble -x dokkaHtml -x dokkaHtmlJar -x javadoc -x dokkaJavadocJar publishToMavenLocal